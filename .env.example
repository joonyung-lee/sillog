# Docker Compose environment variables
# Copy this file to .env and fill in the values

# Spring profile
SPRING_PROFILES_ACTIVE=prod

# Webhook API key (generate with: openssl rand -hex 32)
SILLOG_WEBHOOK_API_KEY=